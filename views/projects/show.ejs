<% include ../partials/header %>

    <h1>
        Project: <%= proj.title %>
    </h1>
    <p>
        <%= proj %>
    </p>
    <hr>
    <p><strong>Add a comment:</strong></p>
    <!-- <form action="/<%= currentUser.username %>/projects/<%= proj._id %>/comments" method="POST"> -->
    <form>
        <textarea rows="3" cols="100" placeholder="..."></textarea>
        <br><button type="button" class="btn btn-primary">Submit</button>
    </form>
<!--
    <% proj.comments.forEach(function(comment) { %>
        <p><%= comment.author.username %> - <%= comment.text %></p>
    <% }) %> -->
<% include ../partials/footer %>
