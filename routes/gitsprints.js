// // git sprints creation/deletion/updating routes
// // CORRESPONDS TO -- // app.use("/:gitsprint", gitsprintRoutes);

// const express = require("express");
// const router = express.Router();
// const authMiddleware = require('../middleware/auth');
// // const gitsprintController = require('../controllers/gitsprintController');

// // do gitsprint creation check (can a gitsprint created with this new project entry)
// // router.post('/', [gitsprintController.checkSprintAvailability, gitsprintController.createNewGitsprint])

// // specific gitsprint show page
// // router.get('/:id', authMiddleware.isLoggedIn, gitsprintController.showGitsprintPage)

// module.exports = router